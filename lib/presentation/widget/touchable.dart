import 'package:flutter/material.dart';

class Touchable extends StatelessWidget {
  const Touchable({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [],
    );
  }
}
